module ymaps {
    export module util {
        export function augment(ChildClass:Function, ParentClass:Function, override:Object):Object;

        // todo wtf string "вуашту"-"define" doing in docs? report?
    }
}