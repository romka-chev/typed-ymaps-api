module ymaps {
    export module geoObject {
        export module addon {
            export class hint {
                static get(geoObject:interfaces.IGeoObject):interfaces.IPopupManager;
            }
        }
    }
}