module ymaps {
    export module map {
        export module action {
            export class hint{

                // todo check map parameter?
                get():interfaces.IPopupManager;
            }
        }
    }
}