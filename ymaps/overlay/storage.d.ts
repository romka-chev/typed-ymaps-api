module ymaps {
    export module overlay {
        class StorageAsyncStorage extends util.AsyncStorage{
            // todo determine all keys?
        }
        export var storage:StorageAsyncStorage;
    }
}