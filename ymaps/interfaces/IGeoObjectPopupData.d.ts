namespace ymaps.interfaces {

    // todo wtf is that and for that is this needed?
    interface IGeoObjectPopupData {
        geometry:IGeometry;
        geoObject:IGeoObject;
        properties:IDataManager;
        userData:any;
    }
}